<div style="width:500px">

<%= form_for @wedding, :html => {:multipart => true} do |f| %>
  <% if @wedding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wedding.errors.count, "error") %> prohibited this wedding from being saved:</h2>

      <ul>
      <% @wedding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "form-control" %>
  </div><br>

  <div class="field">
    <%= f.label :wedding_date %><br>
    <%= f.datetime_select :wedding_date, :class => "form-control" %>
  </div><br>
  <div class="field">
    <%= f.file_field :image, :class => "form-control" %>
  </div><br>
  <div class="field">
    <%= f.label :remote_image_url, "or image URL" %><br />
    <%= f.text_field :remote_image_url, :class => "form-control" %>
  </div><br>

  <div class="field">
    <%= f.label :languages, "Språk" %><br />
    <% @languages.each do |language| %>
      <%= check_box_tag("languages[]", language.id, @weddings_languages.include?(language)) %> <%= language.name %><br>
    <% end %>
  </div><br>

  <div class="actions">
    <%= f.submit "Save", :class => "btn btn-success" %>
  </div><br>
<% end %>

</div>
